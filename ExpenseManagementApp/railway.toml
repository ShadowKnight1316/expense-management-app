[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/expense-management-app-1.0.0.jar --spring.profiles.active=prod"
healthcheckPath = "/api/v1/auth/health"
restartPolicyType = "ON_FAILURE"
